<UserControl x:Class="WpfApp1.view.EmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="900"
             FontFamily="Montserrat">

        <UserControl.Resources>
            <!-- ComboBox ToggleButton Template -->
            <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="20"/>
                    </Grid.ColumnDefinitions>
                    <Border x:Name="Border" 
                        Grid.ColumnSpan="2"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="2"/>
                    <Path x:Name="Arrow"
                      Grid.Column="1"
                      Fill="#FFFFFF"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Data="M0,0 L4,4 8,0 Z"/>
                </Grid>
            </ControlTemplate>

            <!-- ComboBox Template -->
            <ControlTemplate x:Key="ComboBoxTemplate" TargetType="ComboBox">
                <Grid>
                    <ToggleButton x:Name="ToggleButton"
                            Template="{StaticResource ComboBoxToggleButton}"
                            Grid.Column="2"
                            Focusable="false"
                            Background="#C9113752"
                            BorderBrush="#556677"
                            IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                            ClickMode="Press"/>
                    <ContentPresenter x:Name="ContentSite"
                                Content="{TemplateBinding SelectionBoxItem}"
                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                Margin="6,2"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left"/>
                    <Popup x:Name="Popup"
                      Placement="Bottom"
                      IsOpen="{TemplateBinding IsDropDownOpen}"
                      AllowsTransparency="True"
                      Focusable="False"
                      PopupAnimation="Slide">
                        <Border x:Name="DropDownBorder"
                          Background="#A1113752"
                          BorderThickness="1"
                          BorderBrush="#556677"
                          CornerRadius="4">
                            <ScrollViewer x:Name="DropDownScrollViewer">
                                <Grid RenderOptions.ClearTypeHint="Enabled">
                                    <ItemsPresenter/>
                                </Grid>
                            </ScrollViewer>
                        </Border>
                    </Popup>
                </Grid>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="true">
                        <Setter Property="BorderBrush" Value="#3DB3B3"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>

            <!-- ComboBoxItem Template -->
            <ControlTemplate x:Key="ComboBoxItemTemplate" TargetType="ComboBoxItem">
                <Border Background="{TemplateBinding Background}" 
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    BorderThickness="{TemplateBinding BorderThickness}"
                    Padding="6,2">
                    <ContentPresenter/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsHighlighted" Value="True">
                        <Setter Property="Background" Value="#1E474A"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="#0F3542"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>

            <!-- ComboBox Style -->
            <Style TargetType="ComboBox">
                <Setter Property="Background" Value="#113752"/>
                <Setter Property="Foreground" Value="#D5CFF5"/>
                <Setter Property="BorderBrush" Value="#556677"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Template" Value="{StaticResource ComboBoxTemplate}"/>
            </Style>

            <!-- ComboBoxItem Style -->
            <Style TargetType="ComboBoxItem">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="#D5CFF5"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template" Value="{StaticResource ComboBoxItemTemplate}"/>
            </Style>
        </UserControl.Resources>
        <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Çalışanlar"
                   FontSize="30"
                   FontWeight="Bold"
                   Foreground="#FFFFFF"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Grid Width="200" Margin="0,0,15,0">
                <TextBox x:Name="SearchTextBox"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="14"
                         Background="#C9113752"
                         Foreground="#FFFFFF"
                         BorderBrush="#556677"
                         Padding="6"/>
                <TextBlock Text="İsim veya Sicil No ara"
                           Foreground="#666"
                           IsHitTestVisible="False"
                           Margin="10,0,0,0"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchTextBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <ComboBox Width="200"
                      ItemsSource="{Binding CategoryList}"
                      SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
                      Margin="0,0,15,0"
                      FontSize="14"/>

            <ComboBox Width="200"
                      ItemsSource="{Binding DepartmentList}"
                      SelectedItem="{Binding SelectedDepartment, Mode=TwoWay}"
                      Margin="0,0,15,0"
                      FontSize="14"/>

            <Button Content="Filtreleri Temizle"
                    Width="160"
                    FontWeight="SemiBold"
                    Foreground="#089E97"
                    Background="#1E474A"
                    BorderThickness="0"
                    Cursor="Hand"
                    Padding="8,4"
                    Click="ClearFilters_Click"/>
        </StackPanel>

        <Border Grid.Row="2"
                Background="#B11A2E3B"
                Padding="20"
                CornerRadius="12"
                BorderBrush="#556677"
                BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Employees}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#114050"
                                    Padding="14"
                                    Margin="0,0,0,12"
                                    CornerRadius="10"
                                    BorderBrush="#556677"
                                    BorderThickness="1">
                                <Expander Expanded="EmployeeExpander_Expanded"
                                          Cursor="Hand">
                                    <Expander.Header>
                                        <StackPanel Orientation="Vertical">
                                            <TextBlock Text="{Binding FullName}"
                                                       FontSize="18"
                                                       FontWeight="Bold"
                                                       Foreground="#D5CFF5"/>
                                            <WrapPanel>
                                                <TextBlock Text="Sicil No: " FontWeight="SemiBold" Foreground="#3DB3B3"/>
                                                <TextBlock Text="{Binding RegistrationNumber}" Foreground="#88A0D0" />
                                                <TextBlock Text=" | Departman: " FontWeight="SemiBold" Foreground="#3DB3B3" Margin="10,0"/>
                                                <TextBlock Text="{Binding DepartmentName}" Foreground="#88A0D0" />
                                                <TextBlock Text=" | Pozisyon: " FontWeight="SemiBold" Foreground="#3DB3B3" Margin="10,0"/>
                                                <TextBlock Text="{Binding Email}" Foreground="#88A0D0" />
                                            </WrapPanel>
                                        </StackPanel>
                                    </Expander.Header>
                                    <StackPanel Margin="0,10,0,0">
                                        <TextBlock Text="Zimmetli Eşyalar"
                                                   FontSize="14"
                                                   FontWeight="SemiBold"
                                                   Foreground="#A3B1D1"
                                                   Margin="0,0,0,8"/>
                                        <StackPanel>
                                            <ItemsControl ItemsSource="{Binding AssignedItems}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Background="#0F3542"
                                                                Padding="8"
                                                                Margin="0,0,0,6"
                                                                CornerRadius="6"
                                                                BorderBrush="#3A4F5F"
                                                                BorderThickness="1">
                                                            <WrapPanel>
                                                                <TextBlock Text="{Binding CategoryName}" Foreground="#D5CFF5" FontWeight="SemiBold"/>
                                                                <TextBlock Text=" | " Foreground="#3DB3B3" Margin="6,0"/>
                                                                <TextBlock Text="{Binding Name}" Foreground="#88A0D0"/>
                                                                <TextBlock Text=" | Barkod: " Foreground="#3DB3B3" Margin="10,0"/>
                                                                <TextBlock Text="{Binding Barcode}" Foreground="#88A0D0"/>
                                                                <TextBlock Text=" | Seri: " Foreground="#3DB3B3" Margin="10,0"/>
                                                                <TextBlock Text="{Binding SerialNumber}" Foreground="#88A0D0"/>
                                                                <TextBlock Text=" | Zimmet: " Foreground="#3DB3B3" Margin="10,0"/>
                                                                <TextBlock Text="{Binding AssignedAt, StringFormat={}{0:dd.MM.yyyy}}" Foreground="#88A0D0"/>
                                                            </WrapPanel>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                            <TextBlock Text="Zimmetli eşya bulunamadı."
                                                       Foreground="#88A0D0"
                                                       FontStyle="Italic"
                                                       HorizontalAlignment="Left"
                                                       Margin="0,4,0,0">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding AssignedItems.Count}" Value="0">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>
                                </Expander>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>